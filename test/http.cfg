

array set ::config [list]

array set ::config {
  ssl 0
}

set ::config(sites.allowed) {
  *
  duckduckgo.com
  facebook.com
  mail.qq.com
  duck.co
  google
}

return

set config [dict create]

dict set config shadow 0

# use host.allow
# use host.deny
dict set config sites.allowed {
  duckduckgo.com
  facebook.com
  mail.qq.com
  duck.co
}

return $config

return {
  proxy {
    ...
  }

  proxy {
    ...
  }
}
